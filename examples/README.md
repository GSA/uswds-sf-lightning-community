# Examples

```sh
sfdx force:community:create -n "USWDS Demo" -d "Example implementation of the USWDS Theme" -t "Customer Account Portal" -p "" -u {username}
```

```sh
sfdx force:org:open -p _ui/networks/setup/SetupNetworksPage -u {username}
```
